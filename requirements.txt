torch>=2.6.0  # install with: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torchvision>=0.17.0
monai[all]>=1.3.0
nibabel>=5.0.0
scikit-image>=0.22.0
matplotlib>=3.8.0
plotly>=5.20.0
pyyaml>=6.0
tqdm>=4.66.0
wandb>=0.16.0
numpy>=1.26.0
scipy>=1.12.0
SimpleITK>=2.3.0

# Data acquisition & conversion (install as needed)
# synapseclient>=3.0.0   # BraTS download from Synapse
# idc-index>=0.5.0       # TCIA/IDC download
# pydicom>=2.4.0         # DICOM metadata reading in convert_dicom.py
# dcm2niix               # DICOM→NIfTI  (conda install -c conda-forge dcm2niix)
# hd-bet                 # Skull stripping  (pip install hd-bet)
